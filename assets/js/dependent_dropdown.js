var Drupal = Drupal || {};

(function ($, Drupal) {
  "use strict";

  /**
   * @type {{attach: Drupal.behaviors.dependentDropdown.attach}}
   */
  Drupal.behaviors.dependentDropdown = {
    attach: function (context, settings) {

    }
  };

})(jQuery, Drupal);
